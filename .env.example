# Client-side (safe to expose, protected by RLS)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-side only (Supabase Edge Function secrets, set via `supabase secrets set`)
# Never expose these to the client.
ANTHROPIC_API_KEY=
SUPABASE_SERVICE_ROLE_KEY=
